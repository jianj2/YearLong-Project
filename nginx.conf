http {
    server {
    #access_log  /var/log/nginx/host.access.log  main;
    listen 3000;

        location / {
            root /var/www/client/build;
            index index.html;
            try_files $uri /index.html;
        }
    }
}

events { }
